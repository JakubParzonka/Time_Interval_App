<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_info_panel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:id="@+id/device_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="We are the robots!"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/description_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/device_name"
        android:layout_marginBottom="20dp"
        android:text="We are the robots!"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/product_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/description_number"
        android:layout_marginBottom="20dp"
        android:text="We are the robots!"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/serial_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/product_id"
        android:layout_marginBottom="20dp"
        android:text="We are the robots!"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/device_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/serial_number"
        android:layout_marginBottom="20dp"
        android:text="We are the robots!"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/error_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/device_number"
        android:layout_marginBottom="20dp"
        android:text="Here shouldn't be an error"
        android:textSize="20sp" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:onClick="onRefreshButtonClick"
        android:text="Refresh" />


</RelativeLayout>
